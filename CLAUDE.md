# gomi_app — ゴミアプリ制作シリーズ

小さくて実用性が低くて、でも触ると楽しいアプリを積んでいくリポジトリ。

## リポジトリ規約

- 新しいアプリは `apps/<name>/` に作る
- **素の HTML / CSS / ES モジュールのみ。ビルドツール・npm・フレームワークは使わない**
- ルート `index.html` にリンクを1行追加する
- GitHub Pages にそのまま乗る構成を保つ
- 音源・画像などのバイナリ素材は原則コミットしない

## apps/giongen — 汎用擬音ジェネレーター

### コンセプト（設計の前提。ここを踏み外さないこと）

**文字に書き起こせない音を、文字から作る。**

擬音とは「音を文字で近似したもの」なので、このアプリはその**逆変換**をやる。
入力されたかなは**発音しない**。文字は音響パラメータの供給源として扱う。

```
かな → モーラ分割 → 音象徴トレイト（8次元ベクトル）→ 各スタイルが音にする
```

過去に一度「かなをフォルマント合成で発音する」設計で作り、
「周りに音がついて喋っているだけに聞こえる」と却下されている。
**声を鳴らしてよいのは「声の質感」を意図的に使うスタイルだけ。**

### 音象徴トレイト

日本語の音韻はもともと音の性質を符号化している。それを数値にする。

| 文字の特徴 | トレイト |
|---|---|
| 調音位置 ぱ(.15) → た(.50) → か(.85) | bright（明るさ） |
| 破裂音 = .02 / 摩擦音 = .45〜.55 / 鼻音 = .35 | attack（立ち上がりのゆるさ） |
| し(.92) さ(.90) ↔ な(.06) ま(.05) | noisy（ノイズ量） |
| 濁音 → +0.35 | weight（重さ） |
| 母音 い(明1.0/重.1) → お(明.28/重.8) | bright / weight |
| 拗音（きゃ・しゃ） | sweep（ピッチの動き） |
| 促音「っ」 | gap（タメ）＋ 次のモーラに accent |
| 撥音「ん」・長音「ー」 | ring（余韻）・hold（持続） |

### 実測値の扱い

`apps/giongen/data/measurements/*.json` は提供音源の解析結果。
**これは「正解」ではなく「出発点」。**

元音源は BGM・残響・複数レイヤー込みの平均値なので、そのまま再現すると特徴が薄まる。
実際にユーザーから「学習させたものより、オリジナルのほうが特徴を捉えている」と指摘された。
**擬音は忠実再現より戯画化が効く。測定値を基準に 1.3〜1.8倍 誇張してよい。**

### 音源を追加してアプリを更新する手順

1. チャットに音源を投げてもらう
2. 解析する（下記）
3. 測定値を `apps/giongen/data/measurements/<name>.json` に追記
4. 必要なら `apps/giongen/js/styles/*.js` にプリセットを追加
5. `apps/giongen/test.html` のスモークテストを通す
6. commit → push → GitHub Pages が再デプロイ

```bash
python3 -m venv venv && ./venv/bin/pip install -r tools/requirements.txt
cd tools
../venv/bin/python analyze.py   <file>   # EQ・重心・構造・無音区間
../venv/bin/python melody.py    <file>   # 音程・倍音比・アタック/減衰
../venv/bin/python voice.py     <file>   # LPCフォルマント・母音推定
../venv/bin/python rhythm.py    <file>   # BPM・サブベース比率
../venv/bin/python roughness.py <file>   # ジッタ・シマー・HNR・サブハーモニクス
# --json を付けると measurements/ にそのまま貼れる形で出る
```

**音源ファイルはコミットしない。** `.gitignore` で除外済み。
リポジトリに入るのは抽出した数値だけ。これで権利面も容量も問題にならない。

## 解析の落とし穴（実際に踏んだもの。同じ失敗を繰り返さないこと）

1. **逆相ステレオ** — L/R相関が −1 のファイルを単純平均するとほぼ無音になる。
   正常なファイルを2度「壊れている」と誤診した。`common.safe_mono()` を必ず経由する。
2. **ドミナント周波数の追跡** — 低域層と高域層が同時に鳴ると、フレームごとに飛び移って
   **架空のスイープ**を作る。「50msで+63半音」という存在しない値を報告した。
3. **倍音比での調和／非調和判定** — 完全5度(1.50x)や長3度(1.26x)を「非整数だから金属的」と
   誤判定する。和音は複数の調和列の重ね合わせ。比率は必ず半音数に直してから判断する。
4. **spectral flatness では「がなり」を測れない。** ジッタ／HNR／サブハーモニクスを使う。
5. **アタック時間** — 測定窓に次の音が入ると壊れる（166msの音に「アタック344ms」等）。
6. **低いf0のジッタ** — 150Hz未満では30ms窓に周期が数個しか入らず信頼度が落ちる。
7. **n<10 で「確実性A」を名乗らない。** 5素材で「例外なし」と断定してセブフラに覆された。

## 権利・安全性

- 解析で得るのは**特徴量のみ**（EQ・BPM・エンベロープ・共鳴比率・フォルマント）
- 既存楽曲（入店音・チャイム・ゲーム音等）は**メロディ自体が著作物**。固定ジングルとして再現しない。
  音色と質感だけを使い、**音高は入力文字列から生成する**
- ネットミーム由来のスタイルは**中立名**を付け、元ネタのフレーズをアプリ内に書かない
- **警報音系（緊急地震速報風）は隔離する**：おまかせ判定の候補から除外、デフォルト非表示、
  選択時に警告、音量を下げる。ちびっこが偶然鳴らす事故を防ぐため
